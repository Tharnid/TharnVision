Just a list of commands that I use to make it easier lol

git add -A
git commit -m ""
git branch
git status
git checkout -b
git checkout master
git push
git remote -v
git push heroku master

heroku login
heroku logs
heroku create
heroku config:set NODE_ENV=production
heroku ps:scale=1
heroku open
heroku logs

npm install --save
npm install --save-dev
npm install -g

node
nodemon server.js (I use nodemon)
set NODE_ENV=production
set NODE_ENV=development

bower
bower install jquery
bower install toastr --save
bower install angular angular-resource angular-route --save

mongolab
cli mongo ds027829.mongolab.com:27829/tharnvision -u <dbuser> -p <dbpassword>

'use strict'
